generator client {
  provider   = "prisma-client-js"
  output     = "../.prisma/client"
  engineType = "binary"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model example {
  id   Int    @id @default(autoincrement())
  text String
}

model L1Nodes {

}

model L1Edges {
  startNodeID     L1Nodes @id
  endNodeID       L1Nodes @id
  
}
